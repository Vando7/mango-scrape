[project]
name = "deep-dive-shim"
version = "0.1.0"
description = "Thin stdio MCP shim forwarding to the deep-dive scraper service."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2",
    "httpx>=0.27",
]
